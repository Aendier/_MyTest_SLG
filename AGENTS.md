# `_MyTest_SLG` Agent Rules

这些规则适用于 `_MyTest_SLG` 及其所有子目录。先阅读本文件，再阅读与当前任务直接相关的 `README.md` 或代码注释。

## 工作边界

- 这是 Unity 个人工具与实验目录。优先复用现有工具、菜单入口、数据格式和目录结构；不要为了单次任务引入新的框架或依赖。
- `Editor/` 放 Unity 编辑器扩展，`Scripts/` 放运行时代码，`Scene/` 放场景，`Template/` 放示例资源，`Plugins/` 放第三方或独立插件。新文件放入语义匹配的目录。
- 只修改当前任务涉及的文件。不要修改 `_MyTest_SLG/.git`、工程外文件或无关目录，也不要把 `Library/`、缓存、临时导出物加入版本库。

## Unity 资源安全

- Unity 资源的路径与 `.meta` 是一对。移动、重命名或删除资源时，必须同步确认对应 `.meta`；新增资源时确认 Unity 已生成 `.meta`。
- 修改场景、Prefab、材质、Importer 设置或 ScriptableObject 前，先检查引用关系和现有序列化字段；保留无关字段、对象引用、GUID 和文件格式。
- 能通过 Unity Editor 或本目录已有工具完成的操作，优先走现有入口；直接手改 Unity YAML 只用于明确、可复现且无法通过 Editor 完成的变更。
- 生成图片、图集、备份或批处理产物前，确认输出目录和覆盖范围；默认写入任务专用目录，不覆盖源资源。

## C# 与编辑器代码

- 遵循同目录现有命名、命名空间、菜单路径、序列化字段和错误提示风格；公共 API 的改名或删除必须先检查调用方。
- 编辑器代码要考虑 Unity Domain Reload、重复打开窗口、编译失败和对象为空等生命周期情况；异步或批处理操作必须能报告进度、失败原因和取消后的状态。
- 保持改动小而完整：删除由本次改动造成的无用成员，但不要顺手清理无关旧代码。注释只解释不明显的约束或原因。

## 执行流程

1. **定位**：确认目标文件、调用方、资源引用和当前工作区状态。完成条件：能列出本次会修改的文件及其原因。
2. **实现**：按现有模式做最小修改，必要时补充针对性测试或验证入口。完成条件：代码、资源和配套 `.meta` 均处于一致状态。
3. **验证**：检查 `git diff`、新增/删除文件和 `.meta` 配对；C# 改动运行可用的编译或 EditMode 测试；资源改动检查序列化文本和引用未意外变化。完成条件：验证结果明确记录为通过、失败或未执行。
4. **交付**：用中文说明改动文件、行为变化、验证命令/结果和剩余风险。完成条件：其他 agent 可以仅凭摘要复现检查并继续工作。

## 不确定性与失败处理

- 不确定文件归属、资源引用、预期行为或破坏性影响时，先查代码、配置和版本差异；仍无法判断时暂停并说明具体阻塞点，不凭空补全业务规则。
- 工具、Unity 或测试失败时保留原始错误和复现步骤；不要用删除资源、重置工作区或跳过检查来掩盖失败。
- 任务要求与现有 README、配置或调用方冲突时，以可验证的代码事实为准，并在交付摘要中指出冲突。
